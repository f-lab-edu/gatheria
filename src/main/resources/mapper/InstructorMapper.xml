<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gatheria.mapper.InstructorMapper">

    <!-- 교수 삽입 -->
    <insert id="insertInstructor">
        INSERT INTO instructors (email, password, name, phone, affiliation, is_active, created_at, updated_at)
        VALUES (#{email}, #{password}, #{name}, #{phone}, #{affiliation}, #{isActive}, NOW(), NOW())
    </insert>

</mapper>